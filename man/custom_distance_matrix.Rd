% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/location_functions.R
\name{custom_distance_matrix}
\alias{custom_distance_matrix}
\title{Build a Distance Matrix from a Custom Data Frame}
\usage{
custom_distance_matrix(
  df,
  unit = c("miles", "kilometers", "meters"),
  label_var,
  lat_var,
  long_var
)
}
\arguments{
\item{df}{A \code{data.frame} containing label and coordinate columns.}

\item{unit}{Character string; one of \code{"miles"} (default),
\code{"kilometers"}, or \code{"meters"}.}

\item{label_var}{Character scalar; column name to use for matrix
row/column names. Values in this column must be unique and non-missing.}

\item{lat_var}{Character scalar; column name containing latitude values.}

\item{long_var}{Character scalar; column name containing longitude values.}
}
\value{
A list with:
\describe{
  \item{loc_vec}{Character vector of location labels (same order as matrix
  dimensions)}
  \item{distance_matrix}{Square numeric matrix of pairwise distances in
  requested units}
}
}
\description{
Generates an all-pairs distance matrix from latitude/longitude coordinates
in a user-supplied data frame. Row and column names of the matrix are set
from a unique label variable.
}
\examples{
\dontrun{
md <- tract_generator("24")
dm <- custom_distance_matrix(
  md,
  label_var = "geoid", lat_var = "lon", long_var = "long"
)
dim(dm[["distance_matrix"]])

names(md) <- c("tract_id", "lat", "lon")
dm_km <- custom_distance_matrix(
  md,
  unit = "kilometers",
  label_var = "tract_id",
  lat_var = "lat",
  long_var = "lon"
)
}
}
