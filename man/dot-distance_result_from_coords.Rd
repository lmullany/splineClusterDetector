% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/location_functions.R
\name{.distance_result_from_coords}
\alias{.distance_result_from_coords}
\title{Helper function takes a vector of locations, and a set of coords
(which must be a matrix or frame with first two columns being longitude
and latitude), and returns a square distance matrix for all pairs
of coordinates in a given unit}
\usage{
.distance_result_from_coords(
  loc_vec,
  coords,
  unit = c("miles", "kilometers", "meters")
)
}
\arguments{
\item{loc_vec}{Character vector of location identifiers used as matrix
row/column names.}

\item{coords}{Matrix-like object with longitude in column 1 and latitude in
column 2.}

\item{unit}{Character scalar unit for returned distances; one of
\code{"miles"}, \code{"kilometers"}, or \code{"meters"}.}
}
\value{
A list with elements \code{loc_vec} and \code{distance_matrix}.
}
\description{
Helper function takes a vector of locations, and a set of coords
(which must be a matrix or frame with first two columns being longitude
and latitude), and returns a square distance matrix for all pairs
of coordinates in a given unit
}
\keyword{internal}
